#

export SSH_AUTH_SOCK=$(launchctl getenv SSH_AUTH_SOCK_METHOD1)
export SSH_AGENT_PID=$(launchctl getenv SSH_AGENT_PID_METHOD1)
for c in scp sftp slogin ssh ssh-add ssh-agent ssh-keygen ssh-keyscan; do
  alias $c=/usr/local/bin/$c
done

# end of file
